{
  "name": "delivery-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start",
    "deploy": "firebase deploy --only hosting",
    "dev": "npm dev",
    "build": "npm build && npm export",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm start",
    "lint": "npm lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^9.12.0"
  },
  "devDependencies": {
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
